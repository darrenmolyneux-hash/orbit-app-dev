SELECT
  site_id,
  address1,
  address2,
  city,
  postcode,
  site_contact,
  contact_number,
  contact_email
FROM sites
WHERE customer_id = {{appsmith.URL.queryParams.customerId}}
ORDER BY site_id;