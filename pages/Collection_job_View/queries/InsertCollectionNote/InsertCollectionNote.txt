INSERT INTO job_notes (
    jobid,
    note_text,
    created_by
)
VALUES (
    {{ Number(Collection_Ref.text.replace("Collection:", "").trim()) }},
    {{ NotesInput.text || NotesInput.value }},
    {{ appsmith.user.email.split("@")[0] }}
)
RETURNING *;