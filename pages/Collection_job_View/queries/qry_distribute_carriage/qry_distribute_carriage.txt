UPDATE assets
SET carriage_cost_allocated = ROUND({{Number(appsmith.store.miscCostInput || 0)}} / (SELECT COUNT(*) FROM assets WHERE collection_id = {{Number(appsmith.URL.queryParams.collectionId)}}), 2)
WHERE collection_id = {{Number(appsmith.URL.queryParams.collectionId)}};