INSERT INTO models (model_name, make_id, weight)
VALUES (
    {{ ModelNameInput.text }},
    {{ sel_make.selectedOptionValue }},
    {{ Number(NewModelWeightInput.text) }}
)
RETURNING model_id, weight;
