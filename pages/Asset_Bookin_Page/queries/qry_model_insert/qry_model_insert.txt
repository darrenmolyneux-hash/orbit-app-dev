INSERT INTO models (model_name, make_id, weight)
VALUES (
  '{{FormatUtils.titleCase(inp_new_model_name.text)}}',
  {{Number(appsmith.store.reviewContext?.make_id || Custom3.model.selectedMakeId)}},
  {{Number(inp_new_model_weight.text)}}
)
RETURNING model_id, model_name, weight;