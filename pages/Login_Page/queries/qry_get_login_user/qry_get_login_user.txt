SELECT user_id, email, full_name, role, is_active, password_hash, failed_login_attempts, locked_until, must_change_password, last_login
FROM users
WHERE LOWER(email) = LOWER({{this.params.email}})
LIMIT 1;