SELECT 
    s.sale_id,
    s.sale_ref,
    s.customer AS customer_id,
    c.customername AS customernamename,
    s.sale_date,
    s.total_price,
    s.created_by
FROM sales s
LEFT JOIN customers c 
    ON c.customer_id = NULLIF(s.customer, '')::int
ORDER BY s.sale_date DESC;
