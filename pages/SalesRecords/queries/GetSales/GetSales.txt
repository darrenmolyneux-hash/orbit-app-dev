SELECT 
    s.sale_id,
    s.sale_ref,
    s.customer AS customer_id,
    COALESCE(sc.customer_name, s.customer) AS customer_name,
    s.sale_date,
    s.total_price,
    s.created_by
FROM sales s
LEFT JOIN sale_customers sc ON sc.customer_id::text = s.customer
ORDER BY s.sale_date DESC;