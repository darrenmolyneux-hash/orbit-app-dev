CREATE TABLE activity_log (
  log_id SERIAL PRIMARY KEY,
  event_time TIMESTAMP DEFAULT NOW(),
  entity_type TEXT,
  entity_id INT,
  event_type TEXT,
  old_value TEXT,
  new_value TEXT,
  message TEXT,
  changed_by TEXT
);