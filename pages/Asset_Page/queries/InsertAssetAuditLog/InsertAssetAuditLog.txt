INSERT INTO asset_audit_log
(asset_id, field_name, old_value, new_value, changed_by)
VALUES
(
  {{ qry_GetAssetById.data[0].asset_id }},
  'status',
  {{ AssetState.oldStatus }},
  {{ StatusDropdown.selectedOptionValue }},
  {{ appsmith.user.email }}
);
