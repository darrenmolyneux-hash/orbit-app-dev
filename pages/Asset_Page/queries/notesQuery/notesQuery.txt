SELECT 
  note_id,
  note_text,
  created_by,
  TO_CHAR(created_at, 'DD Mon YYYY HH24:MI') AS created_at
FROM asset_notes
WHERE entity_type = 'asset'
AND   entity_id   = {{appsmith.URL.queryParams.assetId}}
ORDER BY created_at DESC