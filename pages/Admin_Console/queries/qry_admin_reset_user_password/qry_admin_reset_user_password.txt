UPDATE users
SET password_hash = crypt('{{this.params.tempPassword}}', gen_salt('bf')),
    must_change_password = true,
    failed_login_attempts = 0,
    locked_until = NULL
WHERE user_id = {{Number(Custom_Admin.model.row.user_id)}};